name: Olá mundo
on: [push]
  # push:
  #   branches: [ "main" ]
  # pull_request:
  #   branches: [ "main" ]
    
jobs:
  dev:   
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        
      - name: Olá mundo
        run: echo "Olá mundo!"
  release:   
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        
      - name: Olá mundo
        run: echo "Olá mundo!"

     